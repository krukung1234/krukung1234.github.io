let games=[],active='all';const grid=document.getElementById('games'),search=document.getElementById('searchInput'),chipsBox=document.getElementById('chips'),countText=document.getElementById('countText'),empty=document.getElementById('empty');function safe(t=''){return String(t).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}function isHidden(s){return s==='hidden'||s==='ซ่อน'}function isSoon(s){return s==='soon'||s==='เร็ว ๆ นี้'}async function load(){try{await firebaseReady;const s=await gamesRef.orderBy('createdAt','desc').get();games=s.docs.map(d=>({id:d.id,...d.data()}));}catch(e){console.error(e);games=[]}renderChips();render()}function renderChips(){const cats=['all',...new Set(games.map(g=>g.category).filter(Boolean))];chipsBox.innerHTML=cats.map(c=>`<button class="chip ${c===active?'active':''}" data-c="${safe(c)}">${c==='all'?'ทั้งหมด':safe(c)}</button>`).join('');document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>{active=b.dataset.c;renderChips();render()})}function render(){const key=(search?.value||'').toLowerCase().trim();let data=games.filter(g=>!isHidden(g.status));data=data.filter(g=>(active==='all'||g.category===active)&&((g.title||'').toLowerCase().includes(key)||(g.description||'').toLowerCase().includes(key)||(g.category||'').toLowerCase().includes(key)));countText.textContent=`${data.length} เกม`;empty.classList.toggle('hidden',data.length>0);grid.innerHTML=data.map(g=>{const soon=isSoon(g.status),href=soon?'#':(g.link||g.gameUrl||'#');return `<article class="card"><div class="thumb">${g.image?`<img src="${safe(g.image)}" alt="${safe(g.title)}">`:'🎮'}</div><div class="body"><h3>${safe(g.title||'ไม่มีชื่อเกม')}</h3><p>${safe(g.description||'')}</p><span class="tag">${safe(g.category||'เกม')}</span><a class="play ${soon?'soon':''}" href="${safe(href)}">${soon?'เร็ว ๆ นี้':'▶ เล่นเกม'}</a></div></article>`}).join('')}search?.addEventListener('input',render);load();
